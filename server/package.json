{
  "name": "ssm-server",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:py": "cd .. && uvicorn server.backend.app:app --reload --host 0.0.0.0 --port 8000",
    "frontend:dev": "cd frontend && npm install && npm run dev",
    "frontend:build": "cd frontend && npm install && npm run build"
  },
  "type": "module"
}
