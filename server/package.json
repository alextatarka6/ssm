{
  "name": "ssm-server",
  "version": "2.0.0",
  "private": true,
  "description": "Node.js backend for Section Stock Market",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "frontend:dev": "npm install --prefix frontend && npm run dev --prefix frontend",
    "frontend:build": "npm install --prefix frontend && npm run build --prefix frontend"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.104.1",
    "cors": "^2.8.5",
    "dotenv": "^17.2.2",
    "express": "^4.21.2",
    "morgan": "^1.10.0",
    "pg": "^8.13.1"
  }
}
